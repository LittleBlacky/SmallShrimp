# LLM provider configuration, follow https://docs.litellm.ai/docs/providers for your own provider config
llm:
  provider: openai              # Options: openai, anthropic, minimax, etc.
  model: gpt-4                 # Model to use for generation, follow litellm model field
  api_key: sk-your-openai-api-key-here  # WARNING: Replace with your actual key, never commit real keys!
  api_base: null               # Optional: Custom API endpoint (http:// or https://)
  temperature: 0.7
  max_tokens: 2048

default_agent: pickle

# websearch:
#   # Priority 1: Custom API (highest)
#   custom:
#     enabled: true
#     api_url: "https://your-search-api.com/search"
#     method: GET  # GET or POST
#     headers:
#       Authorization: "Bearer {token}"  # {token} will be replaced
#     params:
#       q: "{query}"  # {query} will be replaced
#       limit: 10
#     results_path: "results"  # JSON path to results array
#     field_mapping:
#       title: "title"
#       url: "link"
#       snippet: "description"
#
#   # Priority 2: Built-in provider (if no custom)
#   provider:
#     name: serpapi  # Options: serpapi, brave, duckduckgo
#     api_key: your-serpapi-api-key  # Get at https://serpapi.com
#
#   # Priority 3: Default fallback (if no provider selected, uses brave with free tier)
#   default: brave

# webread:
#   provider: crawl4ai

# api: {}

# channels:
#   enabled: true
#   telegram:
#     bot_token: your-bot-token
#     allowed_user_ids:
#       - "your_user_id"

# memory:
#   # 向量检索（可选，需安装额外依赖）
#   use_vector: false
#   # embedding 后端：local / api://模型名
#   #   "local" → sentence-transformers (all-MiniLM-L6-v2, 384d)
#   #   "api://text-embedding-3-small" → OpenAI 兼容 API
#   embedding: null
