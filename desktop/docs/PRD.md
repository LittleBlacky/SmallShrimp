# SmallShrimp Desktop — 产品需求文档 (PRD)

> 版本: v0.1 | 日期: 2026-06-25 | 状态: 草案

---

## 1. 产品概述

### 1.1 产品定位

SmallShrimp Desktop 是 SmallShrimp AI Agent 框架的桌面客户端，让用户通过图形界面与本地 AI Agent 进行交互，无需接触命令行。

### 1.2 核心价值

| 痛点（现状） | 解决（桌面端） |
|---|---|
| 必须打开终端输入命令 | 图形界面，点击即用 |
| 配置靠手写 YAML | 可视化配置表单 |
| 会话历史难浏览 | 侧边栏会话列表，点击切换 |
| 工具调用过程不可见 | 实时展示工具调用卡片 |
| 记忆/技能管理靠文件系统 | 面板式管理界面 |

### 1.3 目标用户

- **AI 重度用户**：日常使用多个 LLM，需要一个统一的本地桌面入口
- **开发者**：需要本地 Agent 辅助编码、查资料、执行 Shell 任务
- **非技术用户**：想用 AI Agent 但不愿碰命令行

---

## 2. 功能范围

### 2.1 MVP（最小可行产品）

```
优先级 P0 — 必须实现
```

| 模块 | 功能 | 说明 |
|------|------|------|
| **进程管理** | 一键启动/停止 SmallShrimp Server | 托盘图标常驻，显示运行状态 |
| **聊天界面** | 消息收发 + Markdown 渲染 + 代码高亮 | 类 ChatGPT 的对话体验 |
| **流式输出** | LLM 回复逐字显示 | 通过 WebSocket 推送 |
| **会话管理** | 新建/切换/删除会话 | 左侧会话列表 |
| **Agent 切换** | 下拉选择当前 Agent | 读取 workspace 中的 Agent 列表 |
| **配置面板** | 图形化编辑 Provider / API Key / 默认 Agent | 写回 config.user.yaml |

### 2.2 V1（体验完善）

```
优先级 P1
```

| 模块 | 功能 |
|------|------|
| **工具调用可视化** | 工具调用过程以卡片形式展示（读取文件、执行命令、搜索网页等） |
| **文件拖拽上传** | 拖拽文件到聊天框，自动读取内容或作为附件 |
| **快捷键** | 全局快捷键唤起窗口（如 `Ctrl+Shift+S`） |
| **消息操作** | 复制、重新生成、编辑上一条消息 |
| **主题切换** | 亮色/暗色/跟随系统 |
| **字体设置** | 字号、字体族可调 |
| **Agent 管理** | 新建/编辑/删除 Agent，表单编辑 AGENT.md |

### 2.3 V2（高级功能）

```
优先级 P2
```

| 模块 | 功能 |
|------|------|
| **记忆管理面板** | 可视化查看/搜索/编辑 5 层记忆 |
| **Skill 管理** | 浏览/启用/禁用 Skill |
| **定时任务管理** | 查看/编辑 Cron 任务 |
| **多 Agent 路由** | 可视化查看路由规则 |
| **用量统计** | Token 消耗、会话统计图表 |
| **插件市场** | 浏览安装社区 Skill / MCP Server |
| **自动更新** | 检测新版本，一键更新 |

---

## 3. 技术架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                Electron Desktop                  │
│  ┌─────────────────────────────────────────────┐  │
│  │           Main Process (Node.js)             │  │
│  │  ┌───────────┐  ┌───────────┐  ┌──────────┐ │  │
│  │  │ Python 进程│  │  托盘管理  │  │ 自动更新  │ │  │
│  │  │ 生命周期   │  │  窗口管理  │  │ (electron │ │  │
│  │  │ (spawn)   │  │           │  │ -updater) │ │  │
│  │  └───────────┘  └───────────┘  └──────────┘ │  │
│  └─────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────┐  │
│  │         Renderer Process (前端 UI)           │  │
│  │  React 18 + TypeScript + Tailwind CSS        │  │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌───────────┐  │  │
│  │  │ 聊天  │ │ 会话  │ │ 配置  │ │ 记忆/技能  │  │  │
│  │  └──────┘ └──────┘ └──────┘ └───────────┘  │  │
│  └──────────────────┬──────────────────────────┘  │
│                     │ HTTP REST + WebSocket        │
│  ┌──────────────────▼──────────────────────────┐  │
│  │         SmallShrimp Server (已有)            │  │
│  │  FastAPI + EventBus + Worker + Agent         │  │
│  └─────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

### 3.2 技术选型

| 层面 | 方案 | 理由 |
|------|------|------|
| **桌面壳** | Electron 28+ | 生态最丰富、Node.js 原生管理 Python 子进程、社区成熟 |
| **前端框架** | React 18 + TypeScript | 生态丰富、适合聊天类应用 |
| **样式** | Tailwind CSS + shadcn/ui | 开发快、组件美观 |
| **状态管理** | Zustand | 轻量、适合中小型应用 |
| **Markdown** | react-markdown + rehype-highlight | 消息内容渲染 |
| **WebSocket** | 原生 WebSocket + 自动重连 | 流式消息推送 |
| **打包** | electron-builder → .exe(NSIS) / .dmg / .AppImage | 跨平台分发，NSIS 安装向导 |
| **进程管理** | Node.js `child_process.spawn` | 原生管理 Python 子进程，无需额外 IPC 层 |

### 3.3 IPC 通信设计

```
Renderer (React)              Main Process (Node)          SmallShrimp (Python)
     │                               │                              │
     │── IPC: 'start-server' ───────▶│── child_process.spawn ──────▶│
     │                               │── health check (http) ──────▶│
     │                               │◀── server ready ────────────│
     │◀── IPC: 'server-status' ─────│                              │
     │                               │                              │
     │── HTTP/WS 直连 localhost ─────────────────────────────────▶│
     │  (数据通信不经过 Main Process)  │                              │
```

- **进程生命周期**：Electron Main Process 通过 `child_process.spawn` 管理 Python 子进程，监听 `exit`/`error` 事件
- **Main ↔ Renderer 通信**：通过 Electron IPC（`ipcMain` / `ipcRenderer`）传递进程状态、系统通知、配置读写等控制指令
- **数据通信**：Renderer 通过 HTTP/WebSocket **直连** SmallShrimp Server，不经过 Main Process 中转（零额外延迟）
- **Main Process 职责**：Python 进程管理、窗口管理、托盘、系统通知、自动更新 (electron-updater)、本地文件读写

---

## 4. UI 设计草案

### 4.1 布局

```
┌──────────┬──────────────────────────────────────┐
│  侧边栏   │              主聊天区                 │
│          │                                      │
│  🔍 搜索  │  ┌────────────────────────────────┐  │
│  ─────── │  │  消息历史（滚动）                 │  │
│  🏠 首页  │  │  ┌──────────┐ ┌──────────┐     │  │
│  ├ 会话1  │  │  │ 用户消息   │ │ AI 回复   │     │  │
│  ├ 会话2  │  │  └──────────┘ └──────────┘     │  │
│  ├ 会话3  │  │  ┌─────────────────────────┐   │  │
│  └ ...   │  │  │ 🔧 工具调用: read_file   │   │  │
│          │  │  └─────────────────────────┘   │  │
│  ─────── │  └────────────────────────────────┘  │
│  ⚙️ 设置  │  ┌────────────────────────────────┐  │
│  🧠 记忆  │  │  输入框                      📎  │  │
│  📦 技能  │  │  [Agent: pickle ▾]    [发送 ▶] │  │
│  ⏰ 定时  │  └────────────────────────────────┘  │
└──────────┴──────────────────────────────────────┘
```

### 4.2 设计原则

- **类聊天应用体验**：参考 ChatGPT / Claude Desktop 的交互模式
- **信息密度适中**：不过于稀疏也不拥挤
- **响应式**：窗口最小宽度 800px，支持拖拽调整侧边栏宽度
- **无障碍**：支持键盘导航，屏幕阅读器友好

---

## 5. 模块详细设计

### 5.1 进程管理模块

```
状态机:
  [未启动] ──点击启动──▶ [启动中] ──健康检查通过──▶ [运行中]
      ▲                      │                          │
      └──────崩溃/手动停止────┘                          │
                                                         │
                                                    [运行中] ──错误──▶ [异常]
```

- 启动时自动查找 Python 环境（优先 `.venv`）
- 健康检查：轮询 `/health` 端点，超时 30 秒提示失败
- 崩溃自动重启（可配置，默认开启）
- 托盘图标状态指示：灰色=未启动、黄色=启动中、绿色=运行中、红色=异常

### 5.2 聊天模块

**消息类型：**

| 类型 | 渲染方式 |
|------|----------|
| 普通文本 | Markdown 渲染（支持表格、列表、加粗等） |
| 代码块 | 语法高亮 + 复制按钮 + 语言标签 |
| 流式输出 | 逐字追加，光标闪烁动画 |
| 工具调用 | 折叠卡片（工具名 + 参数 + 结果），可展开 |
| 错误消息 | 红色边框，显示错误详情 |
| 系统消息 | 灰色居中文字（如"Agent 已切换为 pickle"） |

**输入框：**

- 支持 `Shift+Enter` 换行，`Enter` 发送
- 支持 `/` 唤起斜杠命令（`/skill`, `/agent` 等）
- 支持 `@` 提及文件（自动补全路径）
- 粘贴图片自动存为临时文件并发送路径

### 5.3 配置模块

将 `config.user.yaml` 映射为表单：

```yaml
# 原始配置 → 表单控件
default_provider  → 下拉选择
default_agent     → 下拉选择
providers.*.api_key → 密码输入框
providers.*.api_base → 文本输入框
mcp_servers       → 动态列表（添加/删除/编辑）
```

修改后实时写回 `config.user.yaml`，触发 Server 热重载。

### 5.4 会话管理

- 会话列表按最后活跃时间排序
- 支持重命名、删除（确认对话框）
- 会话自动命名（取第一条消息前 30 字）
- 搜索会话（搜索消息内容）

### 5.5 Agent 管理（V1）

将 `AGENT.md`（YAML 头部 + Markdown 正文）映射为表单：

```yaml
# AGENT.md 结构 → 表单控件
---
name: Pickle              → 文本输入
llm:
  provider: deepseek      → 下拉选择（从配置中的 providers 读取）
  model: deepseek-chat    → 文本输入（含常用模型快捷选项）
  temperature: 0.7        → 滑块 (0~2, 步长 0.1)
  context_window: 200000  → 数字输入
tools:                    → 多选复选框（read/write/shell/websearch/subagent...）
---
# 系统提示词正文          → Markdown 编辑器（带预览）
```

**操作流程：**

| 操作 | 行为 |
|------|------|
| 新建 | 填写表单 → 生成 `workspace/agents/<name>/AGENT.md`，自动创建目录 |
| 编辑 | 解析已有 AGENT.md → 回填表单 → 修改后写回（保留手动编辑的正文格式） |
| 删除 | 确认对话框 → 删除 `agents/<name>/` 整个目录 |
| 查看 | 只读预览模式，展示 Agent 元数据 + 渲染后的提示词 |

**入口：** 侧边栏「📦 Agent」→ 列表页（含搜索）→ 点击进入详情/编辑

---

## 6. API 设计（Server 端新增/已有）

### 6.1 已有接口（可直接使用）

| 端点 | 方法 | 用途 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/ws/chat` | WebSocket | 聊天消息流 |
| `/api/agents` | GET | Agent 列表 |

### 6.2 需新增接口

| 端点 | 方法 | 用途 | 优先级 |
|------|------|------|--------|
| `/api/sessions` | GET | 会话列表 | P0 |
| `/api/sessions/:id` | GET | 会话消息历史 | P0 |
| `/api/sessions/:id` | DELETE | 删除会话 | P0 |
| `/api/sessions/:id/rename` | PATCH | 重命名会话 | P1 |
| `/api/config` | GET/PUT | 读写配置 | P0 |
| `/api/memories` | GET | 记忆列表/搜索 | P2 |
| `/api/skills` | GET | Skill 列表 | P2 |
| `/api/crons` | GET | 定时任务列表 | P2 |

---

## 7. 里程碑 & 时间线

| 阶段 | 内容 | 预估工期 |
|------|------|----------|
| **M0: 基础设施** | Electron 项目搭建 (electron-forge/vite)、React 框架、开发环境 | 2 天 |
| **M1: MVP 核心** | 进程管理 + 聊天 + 流式输出 + 会话列表 | 1 周 |
| **M2: MVP 配置** | 配置面板 + Agent 切换 | 3 天 |
| **M3: MVP 收尾** | 托盘图标、打包脚本、基础测试 | 2 天 |
| **🎯 MVP 发布** | 可下载安装，基础聊天可用 | **~2 周** |
| **M4: V1 体验** | 工具调用可视化、快捷键、主题、消息操作 | 1 周 |
| **M5: V2 高级** | 记忆面板、Skill 管理、用量统计、自动更新 | 2 周 |

---

## 8. 风险 & 缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Python 环境检测失败 | 无法启动 Server | 支持手动指定 Python 路径；内置诊断工具 |
| WebSocket 断连 | 消息丢失 | 自动重连 + 消息本地缓存 |
| 跨平台打包复杂 | 延迟发布 | 先聚焦 Windows，Mac/Linux 后续适配 |
| Server API 不满足前端需求 | 前端阻塞 | MVP 阶段通过直接读文件绕过部分 API |

---

## 9. 附录

### 9.1 参考产品

- **Claude Desktop**（Anthropic）：MCP 集成方式
- **ChatGPT Desktop**（OpenAI）：聊天体验
- **Open WebUI**：本地部署的 Web 聊天界面
- **Jan**：开源离线 AI 桌面应用

### 9.2 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-06-25 | v0.1 | 初始草案 |
| 2026-06-25 | v0.2 | 技术选型从 Tauri 切换为 Electron |
| 2026-06-25 | v0.3 | V1 新增 Agent 管理模块（新建/编辑/删除） |
